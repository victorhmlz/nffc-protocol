# NFFC Protocol — environment template.
# Copy to `.env.local` for local development. Never commit real values.
#
# TASK-01 is infrastructure only and needs NO variables to build/lint/typecheck/test.
# Entries are added by the TASKS that introduce them:
#   TASK-04  — RPC endpoints (Robinhood Chain 4663), PostgreSQL, Redis, log level
#   TASK-22  — Chainlink / oracle configuration
#   TASK-06  — Robinhood provider sync credentials
#   TASK-31+ — admin / multisig addresses

# Example placeholder (unused in TASK-01):
# NEXT_PUBLIC_APP_ENV=development
